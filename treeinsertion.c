Void insert()
{
     Struct node *t=root, *ptr=null;
     Int data;
     struct node *n=(struct node*) malloc(sizeof(struct node));
     printf("enter info which u want to enter:");
     scanf("%d",&data);
          n->info=data;
          n->left=null;
          n->right=null;
     
     
}
